# Cloudflare API credentials
# Get these from your Cloudflare account dashboard
CF_API_EMAIL=your.email@example.com
CF_API_KEY=your_cloudflare_api_key

# Domain configuration
# The main domain for your Traefik services
DOMAIN=your-domain.com

# Basic auth for Traefik dashboard
# Generate with: echo $(htpasswd -nb user password) | sed -e s/\\$/\\$\\$/g
# Replace 'user' and 'password' with your desired credentials
TRAEFIK_BASIC_AUTH=user:$$your_hashed_password

# Cloudflared tunnel configuration
# Get the tunnel token from your Cloudflare dashboard after creating a tunnel
TUNNEL_TOKEN=your-tunnel-token 